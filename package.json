{
  "name": "sql-vault",
  "version": "1.0.0",
  "description": "SQL Vault - biblioteca pessoal de consultas SQL (desktop)",
  "private": true,
  "scripts": {
    "php:serve": "php -S 127.0.0.1:8756 index.php",
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
